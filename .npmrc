# Allow optional dependencies to fail
# This prevents platform-specific binaries from breaking builds
optional=true

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=false

# Engine strict mode
engine-strict=false
